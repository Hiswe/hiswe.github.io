<% if (site.tags.length){ %>
<div class="widget widget--tag">
  <h3 class="widget__title"><%= __('tags') %></h3>
  <ul class="widget__entry">
  <% site.tags.sort('name').each(function(item){ %>
    <li><a href="<%- config.root %><%- item.path %>"><%= item.name %></a><small><%= item.posts.length %></small></li>
  <% }); %>
  </ul>
</div>
<% } %>
